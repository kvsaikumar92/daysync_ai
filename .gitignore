# Secrets / local config
.env
.streamlit/secrets.toml
.daysync_private

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Local runtime artifacts
*.log

# Submission assets & writeup (kept local, not part of the source repo)
submission_assets/
WRITEUP.md
PROJECT_DESCRIPTION.md
PROJECT_DESCRIPTION.html

# Runtime data generated by the app (the app recreates these on run).
# Anchored to the repo root with a leading '/' so the committed sample data
# under examples/ is NOT ignored.
/agent_vault.csv
/media/
/knowledge/
